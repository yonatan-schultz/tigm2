<h1>RSVP</h2>



<%= form_for @played_game do |f| %>

  <% if @played_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@played_game.errors.count, "error") %> prohibited this invitation from being saved:</h2>

      <ul>
      <% @played_game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <%= f.check_box :rsvp %>
  <%= f.submit "RSVP" %>


<% end %>


<%= link_to 'Back to Games', games_path %> |   <%= link_to 'Delete Invitation', @played_game, method: :delete, data: { confirm: 'Are you sure?' } %>